#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Версия и имя программы — одни на все модули, страницы и установщик.

Нумерация — SemVer, МАЖОР.МИНОР.ПАТЧ:
  МАЖОР — несовместимые изменения (схема БД без миграции, поломка API/юнитов);
  МИНОР — новые возможности, старое работает как раньше;
  ПАТЧ  — исправления без новых возможностей.

Как выпустить версию: поднять номер здесь, добавить запись в CHANGELOG.md
(дата и что изменилось), перезапустить сервисы. Виджет сам перезагрузит страницу,
увидев новую версию в заголовке X-App-Version. В публичном репозитории — ещё тег vX.Y.Z.

APP_ID — техническое имя: папки данных (~/.local/share/<APP_ID>), имена сервисов
systemd (<APP_ID>.service, <APP_ID>-widget.service), префикс переменных окружения.
LEGACY_IDS — прежние имена: их папки данных и сервисы при установке переезжают на новое
(paths.migrate_old_app_dirs, install.sh).
"""

APP_NAME = "messhub"
APP_ID = "messhub"
LEGACY_IDS = ("express-msgs",)      # до 1.2.0 проект назывался eXpress-msgs
__version__ = "1.2.0"


def version_line():
    return f"{APP_NAME} {__version__}"
